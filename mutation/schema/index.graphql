#import User, Profile from 'User.graphql'
#import Mutation from 'Mutation.graphql'

scalar Date

type Query {
    users: [User]
    user(id: Int): User
    profiles: [Profile]
    profile(id: Int): Profile
}